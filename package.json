{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "generate:cdn": "node scripts/generate-cdn-js.mjs",
    "build": "tsc && vite build && cp cdn.html dist/ && cp cdn.js dist/ && cp src/style.css dist/cdn.css",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~6.0.3",
    "vite": "^8.0.13"
  },
  "dependencies": {
    "@arcgis/map-components": "^5.1.0-next.120"
  }
}
